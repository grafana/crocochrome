name: Validate renovate config
on:
  pull_request:
    paths:
      - "**renovate*.*"
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout Code
        uses: actions/checkout@eef61447b9ff4aafe5dcd4e0bbf5d482be7e7871 # v4.2.1
      - name: Validate renovate config
        run: |-
          # Renovate updates the line below. Please keep its formatting as it is.
          export RENOVATE_IMAGE=ghcr.io/renovatebot/renovate:39.45.0@sha256:9bc3023dfafb15d9f2aff9362a7fee647d15e663960f0c006657f02c96478adc
          docker run -i --rm -v $(realpath .):/repo -w /repo --entrypoint renovate-config-validator "$RENOVATE_IMAGE" --strict
